# Test: data fields don't have modifiers
data bad_modifier {
    in x: int
    ^^ @@ERROR: Data fields don't have modifiers (remove 'in')
}

# Test: data body with member funcs (func disambiguation)
data with_funcs {
    name: string

    func do_something {}

    func alpha {}
    ^^^^^^^^^^^^^ @@STRICT: 'func alpha' should appear before 'func do_something'
}
