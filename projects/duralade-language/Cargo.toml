[package]
name = "duralade-language"
version.workspace = true
edition.workspace = true

[dependencies]
indexmap = "2"
serde = { version = "1.0", features = ["derive", "rc"] }
tracing = "0.1"

[dev-dependencies]
# test dependencies later
