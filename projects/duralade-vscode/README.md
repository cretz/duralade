# Duralade VSCode Extension

Syntax highlighting and language support for Duralade in Visual Studio Code.

Edit syntax in `syntaxes/duralade.tmLanguage.yaml` and rebuild with `npm run build`.

This extension is in early development and needs significant work on the grammar.
